# Optional dependency for the Moondream label-review and test-comparison pipelines.
# Install only after the CUDA-compatible PyTorch environment is working:
#   uv pip install --upgrade -r requirements-moondream.txt
#
# Moondream 3.1 requires a current Photon/Kestrel runtime. Older installations
# may only recognize `moondream2` and `moondream3-preview`.

moondream>=1.3.0
